<h1><%= @movie.title %></h1><br>
<h2><%= @movie.rating %></h2><br>
<h2><%= @movie.director %></h2><br>
<h2><%= @movie.description %></h2><br>

<%= link_to "Back", root_path %> | <%= link_to "Edit", edit_movie_path(@movie) %> | <%= link_to "Delete", movie_path(@movie), method: :delete, data: {confirm: "You can't fix this. Are you sure you want to delete this movie?"} %>